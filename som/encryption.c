#include<stdio.h>
#include<conio.h>
#include<ctype.h>
#include<string.h>

void main()
{
    char arr[];
    int i,j,k;
    printf("enter the msgending with $:");
    scanf("%s",&arr);
    whuile(a[i]!=$)
    {
       for(i=0;i<26;i++)
       {
        for(j=assici(a);j=assici(z);j++)
        {
         printf(arr[i],arr[j];)
        }
       }
    }
}